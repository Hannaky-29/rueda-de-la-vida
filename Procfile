# Archivo: Procfile
web: gunicorn app:app

# Nota:
# Cambié el comando de inicio en el Procfile para que use 'gunicorn',
# que es el servidor de producción recomendado en Render.
# Render detectará e instalará 'gunicorn' automáticamente.

gunicorn==20.1.0